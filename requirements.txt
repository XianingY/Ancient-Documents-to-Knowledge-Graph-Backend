# =============================================================================
# 古代地契知识图谱 · 后端运行时依赖
# -----------------------------------------------------------------------------
# 版本与当前生产环境 pip list 对齐（直接依赖均用 == 固定，便于换机复现）。
#
# Python：建议 3.11+（与《项目开发文档》一致；networkx 3.6 / pytest 9 等需 3.10+）
#
# 在本目录安装：
#   python -m venv .venv
#   .venv\Scripts\activate          # Windows
#   source .venv/bin/activate       # Linux / macOS
#   python -m pip install -U pip
#   pip install -r requirements.txt
#
# 可选镜像：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 关于 chromadb：勿使用 0.3.x（与 Pydantic 2 冲突）。固定 0.5.23；
# 若 Celery 日志仍报 BaseSettings，请确认已部署 pydantic_chroma_shim（chroma.py 会先加载），
# 并执行 pip install pydantic-settings。
# =============================================================================

# Web
fastapi==0.135.1
starlette==0.52.1
uvicorn[standard]==0.41.0
python-multipart==0.0.22
slowapi==0.1.9

# 配置与校验
python-dotenv==1.2.2
pydantic==2.12.5
pydantic-settings==2.13.1

# 数据库
SQLAlchemy==2.0.48

# 鉴权（PyPI 包名 PyJWT，代码中 import jwt）
PyJWT==2.12.1
passlib==1.7.4
bcrypt==4.0.1

# 图片与本地 OCR
pillow==12.1.1
numpy==2.3.5
opencv-contrib-python==4.10.0.84
paddlepaddle==3.3.1; sys_platform == "darwin"
paddlepaddle==3.3.1; sys_platform == "linux" and platform_machine == "x86_64"
paddlepaddle==3.2.2; sys_platform == "linux" and platform_machine == "aarch64"
paddleocr==3.7.0
paddlex[ocr-core]==3.7.1

# 任务队列与缓存
celery[redis]==5.6.2
redis>=4.5.2,<6.5

# AI / 向量检索 / 图分析
dashscope==1.25.13
chromadb==0.5.23
kubernetes==35.0.0
networkx==3.6.1

# OCR Optimization — Ensemble alignment
sequence_align>=0.2.0
